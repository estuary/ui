import { Container, Toolbar } from '@mui/material';

const Collections = () => {
    return (
        <Container
            sx={{
                pt: 2,
            }}
        >
            <Toolbar />
        </Container>
    );
};

export default Collections;
