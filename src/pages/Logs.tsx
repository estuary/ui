import { Box } from '@mui/material';

const Logs: React.FC = () => {
    return (
        <>
            <Box>This is where we will show the logs for the system.</Box>
        </>
    );
};

export default Logs;
