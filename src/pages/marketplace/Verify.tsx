import GoogleMarketplaceVerify from 'components/marketplace/google/Verify';

// Expanding Marketplace Providers (same idea as the callback wrapper)
function MarketplaceVerify() {
    return <GoogleMarketplaceVerify />;
}

export default MarketplaceVerify;
