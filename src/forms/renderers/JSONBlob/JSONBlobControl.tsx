import { withJsonFormsControlProps } from '@jsonforms/react';
import JSONBlob from './JSONBlob';

const JSONBlobControl = () => <JSONBlob />;

export default withJsonFormsControlProps(JSONBlobControl);
