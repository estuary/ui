export type DataByHourRange = 6 | 12 | 24 | 48;

// TODO (typing): Replace instances of the narrowly scoped type with its broader counterpart.
export type DataByHourStatType = 'docs' | 'bytes';
export type StatType = 'docs' | 'bytes';
