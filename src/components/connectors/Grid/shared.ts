import type { TableIntlConfig } from 'src/types';

export const CONNECTOR_GRID_COLUMNS = { xs: 4, sm: 4, md: 12, lg: 12, xl: 12 };
export const CONNECTOR_GRID_SPACING = { xs: 2, md: 3 };

export const intlConfig: TableIntlConfig = {
    header: 'connectors.main.message1',
    message: 'connectors.main.message2',
};
