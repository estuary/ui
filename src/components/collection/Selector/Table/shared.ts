// Used to control when firing the `setAllSelected` function in the table selector if we should
//  just set the entire object into the map. The key could be anything but wanted to make it
//  something I did not think would ever be a real key
export const EVERYTHING = '👍~e~v~e~r~y~t~h~i~n~g~👍';

export const catalogNameColumn = 'catalog_name';
export const publishedColumn = 'updated_at';
