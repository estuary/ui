export interface EndpointConfigProps {
    hideBorder?: boolean;
    hideWrapper?: boolean;
    readOnly?: boolean;
}

export interface SectionContentProps {
    connectorImage: string;
    readOnly?: boolean;
}
